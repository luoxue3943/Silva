/**
 * PostCSS 配置 / PostCSS configuration
 *
 * 接入 Tailwind CSS v4 的 PostCSS 插件。
 * Wires the Tailwind CSS v4 PostCSS plugin.
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
